#!/bin/sh

repo_root=$(git rev-parse --show-toplevel)
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$repo_root/scripts/sync_skills.ps1"
status=$?

if [ "$status" -ne 0 ]; then
  echo "Skill synchronization failed; commit aborted."
  exit "$status"
fi
