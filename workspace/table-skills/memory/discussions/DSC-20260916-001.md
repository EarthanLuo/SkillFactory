---
id: DSC-20260916-001
type: discussion
created: 2026-09-16
project: table-skills
related:
  - EXT-20260916-001
---

# Discussion

> Status: invalid as a user discussion. This record was created before the user had participated and is retained only as traceable evidence of the behavior reported in `FB-20260916-001`.

## Starting Point

讨论 `EXT-20260916-001` 提取的“可核验的长任务交接”经验，判断它是否适合当前的 Skill Foundry 与 Windows Codex 工作方式。

## User View

用户尚未提供额外立场。本次依据当前项目规则与已知工作上下文进行初步内化：偏好 Markdown-first、低摩擦捕获、可追溯 provenance、保守演进，并要求区分 `skill/` 与 `memory/`。

## Challenges

- 仓库中的交接原则与当前项目的可追溯记录方式一致，但其完整自动化链路依赖宿主的任务 ID、Hook 生命周期、状态查询和跨任务能力。
- 自动提醒和自动接续可能把“记录经验”扩大成中央状态机或永久流程，这与当前项目明确的非自动化边界冲突。
- 交接材料若过度详细，会退化为聊天日志；若过度精简，又可能漏掉授权、版本或验收条件。
- 当前没有真实交接案例来验证“只读核验”能否发现关键差异，也没有独立证据证明网页替身能节省 token。

## What Changed

把候选经验从“安装一个项目交接 Skill”改写为一组可单独测试的判断规则：记录最小恢复事实，显式标注证据等级，先核验再转交修改权，结果未知时先查询，不以 Hook 代替业务判断或用户授权。

## Agreement

没有形成用户确认的 Agreement。以下内容只能视为当时 Agent 的初步分析，不能作为用户立场：原则层可能比仓库的具体脚本、Hook 配置或网页替身方案更适合迁移；任何 Portable Skill 仍需独立、自包含，并且不能依赖 `memory/`。

## Unresolved

- 交接记录的最小字段集合尚未由真实任务验证。
- 尚未确定是否需要一个独立 Portable Skill，还是将原则合并进已有的任务工作方式。
- 尚未验证当前宿主是否能可靠支持创建后的只读核验、消息状态查询和交接幂等性。
- 尚未定义“已接手”的最低证据标准和失败后的人工降级格式。

## Decision

`UNRESOLVED`

### Decision scope

没有发生真实用户讨论，因此不能记录用户决定。原先由 Agent 代写的 `TEST` 已撤回；是否测试、采用、调整或拒绝该经验，须等待用户参与讨论。当前仍不创建或修改 `workspace/table-skills/skill/`。
